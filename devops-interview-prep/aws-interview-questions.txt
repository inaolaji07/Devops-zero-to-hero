Part 1:

1. What is the purpose of a NAT gateway, and what are some of its use cases?
A NAT Gateway allows resources in a private subnet to access the internet without exposing those resources directly to inbound internet traffic.

For example, if I have EC2 instances running in a private subnet, they may still need outbound internet access for things like downloading OS updates, pulling Docker images, or connecting to external APIs. Instead of assigning public IPs to those servers, I route their outbound traffic through a NAT Gateway that sits in a public subnet.

The main use cases are:

Allowing private servers to access the internet securely
Keeping backend systems isolated from direct public access
Supporting patch management and software downloads
Enabling outbound connectivity for application servers in multi-tier architectures

I usually associate NAT Gateways with security best practices because they reduce the attack surface while still allowing controlled outbound communication.
2. How do you decide which load balancer is best suited for an application?
I normally start by understanding the application architecture and traffic type.

If the application needs advanced HTTP or HTTPS routing features like:

path-based routing
host-based routing
SSL termination
WebSocket support

then I would typically choose an Application Load Balancer (ALB).

If the requirement is high performance with very low latency at Layer 4 using TCP or UDP traffic, then I would use a Network Load Balancer (NLB).

I also consider:

scalability requirements
protocol support
security requirements
session persistence needs
cost
whether containerized workloads are involved

For example:

A microservices-based web application usually fits well with an ALB
A gaming service or real-time application that needs ultra-low latency is better suited for an NLB

So the decision mainly depends on traffic type, routing complexity, and performance requirements.
3. What are the key features of the Nginx service?
NGINX is widely used as a web server, reverse proxy, and load balancer.

Some key features are:

Reverse proxy functionality
Load balancing
High performance and low memory usage
SSL/TLS termination
Caching support
URL rewriting and redirects
Static content serving
Support for high concurrency
Integration with containerized and microservices environments

One reason Nginx is popular is its event-driven architecture, which allows it to handle a very large number of simultaneous connections efficiently.

In production environments, I’ve commonly seen it used in front of application servers like Node.js, Java, or Python services to improve performance and distribute traffic.
4. What are the differences between a Network Load Balancer and an Application Load Balancer?
The main difference is the OSI layer at which they operate.

An Application Load Balancer operates at Layer 7, which is the application layer. It understands HTTP and HTTPS traffic and can make routing decisions based on URLs, headers, hostnames, and paths.

A Network Load Balancer operates at Layer 4, which is the transport layer. It routes traffic based on TCP or UDP packets and focuses more on speed and performance.

Key differences:

Application Load Balancer (ALB)	Network Load Balancer (NLB)
Layer 7	Layer 4
Supports HTTP/HTTPS	Supports TCP/UDP
Path-based routing	No content-aware routing
Better for web apps and APIs	Better for high-performance workloads
SSL termination supported	Pass-through or TLS support
More intelligent routing	Lower latency and higher throughput

So in simple terms:

ALB is better for smart web traffic routing
NLB is better for ultra-fast and stable network traffic handling
5. Why is a DynamoDB table used to lock the Terraform state file?
Terraform uses state files to track infrastructure resources.

When teams work collaboratively, multiple users or CI/CD pipelines might try to modify the same infrastructure simultaneously. That can corrupt the Terraform state file or create inconsistent infrastructure changes.

A DynamoDB table is used for state locking to prevent concurrent operations.

For example:

If one engineer is running terraform apply
Another engineer cannot modify the same state until the lock is released

This ensures:

consistency
safe collaboration
prevention of race conditions
protection against state corruption

Typically this is used alongside storing the Terraform state in an S3 backend.
6. How can we manage the Terraform state file effectively?
Some best practices I follow for Terraform state management are:

Store the state remotely instead of locally
usually in an S3 bucket
Enable versioning on the S3 bucket
Use DynamoDB for state locking
Restrict access using IAM policies
Separate state files by environment
dev, staging, production
Avoid manually editing the state file
Use encrypted storage for security
Regularly back up state files
Use Terraform workspaces or separate backends when appropriate

I also make sure sensitive values are protected because the state file can contain secrets or infrastructure metadata.

Good state management is important for team collaboration, reliability, and disaster recovery.


7. If you encounter an error with the `terraform plan` command, how would you troubleshoot it? What steps would you take?
If terraform plan fails, I usually troubleshoot in a structured way.

First, I carefully read the error message because Terraform errors are often descriptive.

My troubleshooting steps would be:

Validate the Terraform syntax
terraform validate
Check formatting issues
terraform fmt
Verify provider authentication and credentials
AWS credentials
IAM permissions
environment variables
Reinitialize Terraform if providers or modules are corrupted
terraform init
Check variable values
missing variables
incorrect types
wrong environment configs
Review recent code changes
compare with previous working versions
check Git history
Enable detailed logs if needed
TF_LOG=DEBUG terraform plan
Verify backend connectivity
S3 access
DynamoDB lock issues
network connectivity
Check resource dependencies and module references

If the issue still persists, I would isolate the problem by testing smaller modules or using targeted plans.

In an actual production environment, I also make sure no unintended infrastructure changes are introduced before proceeding with an apply operation.

8. What stages are available in a Jenkins pipeline? How do you configure a webhook for auto-triggering builds?
9. What DevSecOps tools have you used? How do you secure applications using these tools?
I’ve worked with several DevSecOps tools across CI/CD, container security, infrastructure security, and monitoring.

Some commonly used tools include:

Jenkins for CI/CD pipelines
SonarQube for static code analysis
Trivy and Aqua Security for container scanning
Terraform with security scanning tools like Checkov
Vault for secrets management
Prometheus and Grafana for monitoring and alerting

To secure applications, I usually:

integrate security scans into CI/CD pipelines
scan Docker images for vulnerabilities before deployment
enforce IAM least privilege policies
store secrets securely instead of hardcoding them
enable TLS encryption
monitor logs and metrics continuously
use container runtime security policies
perform dependency vulnerability scanning

The main goal is to shift security left and catch issues early in the deployment lifecycle.
10. How do you manage secrets in a Jenkins pipeline?
11. How do you deploy a microservices application so that each service remains isolated?
I typically isolate microservices at multiple layers:

container level
network level
namespace level
resource level

Using Kubernetes, each microservice runs in its own pod or deployment. I use:

separate namespaces for environments or teams
network policies to control pod-to-pod communication
resource limits and requests to prevent resource exhaustion
separate ConfigMaps and Secrets
independent CI/CD pipelines

For example:

the authentication service should not directly access the payment database
communication should happen only through controlled APIs

This improves:

security
scalability
fault isolation
deployment flexibility

I also prefer using service meshes like Istio when advanced traffic management and service-level security are required.

12. In a Dockerfile, what are the use cases for the `COPY` and `ADD` commands?
Both commands are used to move files into a Docker image, but there are differences.

COPY

COPY is mainly used for straightforward file and directory copying from the local system into the container image.

Example:

COPY package.json /app/

I prefer COPY for most use cases because it’s simpler and more predictable.

ADD

ADD has additional features:

can extract compressed archives automatically
can download files from URLs

Example:

ADD app.tar.gz /app/

However, because ADD has extra behaviors, it can sometimes create unexpected results. In production environments, I usually recommend using COPY unless the advanced functionality of ADD is specifically needed.
13. Write a Dockerfile for a Node.js application, including the build and run commands.
# Base image
FROM node:18

# Create app directory
WORKDIR /app

# Copy package files
COPY package*.json ./

# Install dependencies
RUN npm install

# Copy application source code
COPY . .

# Build application
RUN npm run build

# Expose application port
EXPOSE 3000

# Start application
CMD ["npm", "start"]

docker build -t nodejs-app .

docker run -d -p 3000:3000 nodejs-app
14. Briefly explain Kubernetes architecture, covering each component and its use case.


15. How do you manage connectivity between multiple pods in a Kubernetes cluster?

Pods communicate using Kubernetes networking.

Common approaches include:

Kubernetes Services
DNS-based discovery
Ingress controllers
Network Policies

For example:

A frontend pod can communicate with a backend service using the service DNS name.

Example:

backend-service.default.svc.cluster.local

I also use:

ClusterIP services for internal communication
LoadBalancer services for external access
NetworkPolicies for secure traffic control

In larger environments, service meshes like Istio help with:

traffic routing
mTLS
observability
16. How do worker nodes communicate with the control plane (master node)?

Worker nodes communicate with the control plane mainly through the Kubelet service.

The Kubelet:

registers the node with the cluster
receives instructions from the API server
reports node and pod status

Communication is secured using:

TLS certificates
authentication tokens
RBAC policies

The API Server acts as the central communication hub between all components.
17. How can we increase the capacity of pods using the CLI (command line interface)?
There are several ways depending on the requirement.

Increase replica count manually
kubectl scale deployment my-app --replicas=5
Update resource limits

Edit deployment:

kubectl edit deployment my-app

Then increase:

CPU requests/limits
memory requests/limits
Use Horizontal Pod Autoscaler
kubectl autoscale deployment my-app --cpu-percent=70 --min=2 --max=10

This automatically scales pods based on CPU utilization.
18. What are the use cases of EKS (Elastic Kubernetes Service)?

Amazon EKS is used to run Kubernetes clusters on AWS without managing the control plane manually.

Common use cases:

microservices deployments
container orchestration
CI/CD automation
hybrid cloud deployments
auto-scaling applications
machine learning workloads
high availability production systems

Benefits include:

AWS-managed control plane
integration with IAM, VPC, CloudWatch
improved scalability
reduced operational overhead

It’s useful when organizations want Kubernetes benefits without handling cluster management complexity themselves.
19. If a new version fails, how would you roll it back?
In Kubernetes, rollback is usually straightforward.

First, I would identify:

deployment errors
failed pods
logs
health checks

Then rollback using:

kubectl rollout undo deployment my-app

I also verify rollout history:

kubectl rollout history deployment my-app

In CI/CD pipelines, I usually implement:

blue-green deployment
canary deployment
automated rollback triggers

This minimizes downtime and reduces production impact.


20. What is an Ansible playbook, and how does it work? How does Ansible handle variables differently from playbooks?

Ansible playbooks are YAML files used to automate configuration management, deployments, and orchestration tasks.

A playbook contains:

hosts
tasks
variables
roles

Example workflow:

Ansible connects to target servers using SSH
Executes tasks sequentially
Ensures desired state configuration

Variables in Ansible are dynamic values used inside playbooks.

Variables can come from:

inventory files
group vars
host vars
extra vars
role defaults

Playbooks define what tasks should happen, while variables make those tasks reusable and environment-specific.
21. How do you configure Grafana and Prometheus for monitoring?
Typically:

Install Prometheus
Configure scrape targets
Install Grafana
Connect Grafana to Prometheus as a data source
Import dashboards
Configure alerts

Prometheus collects metrics from:

Kubernetes
EC2
applications
exporters

Example scrape config:

scrape_configs:
  - job_name: 'node-exporter'
    static_configs:
      - targets: ['localhost:9100']

Grafana visualizes the metrics using dashboards.

I usually configure:

CPU monitoring
memory monitoring
pod health
API latency
error rate alerts

Alertmanager is often integrated for Slack or email notifications.
22. What steps would you take if an EC2 instance runs out of memory? Are there specific strategies you'd try first to increase its capacity?
If an Amazon EC2 instance runs out of memory, I first identify the root cause instead of immediately resizing the server.

My troubleshooting steps:

Check memory usage
free -h
top
htop
Identify memory-heavy processes
Review application logs
Check for memory leaks
Restart problematic services if required

Short-term solutions:

clear cache
add swap space
restart services

Long-term solutions:

optimize application memory usage
tune JVM or application configs
move workloads to autoscaling groups
upgrade instance type

If the workload is consistently high, I would vertically scale the EC2 instance or horizontally scale the application.
23. What are the major challenges you've faced in your role? 
One major challenge I faced was handling production deployment failures in a microservices environment.

There was a situation where a new deployment caused increased API latency and intermittent failures across dependent services. The challenge was identifying the root cause quickly without causing major downtime.

What I did:

checked Kubernetes pod logs and events
analyzed Grafana dashboards and Prometheus metrics
verified recent deployment changes
rolled back the deployment immediately to stabilize production
later identified a resource configuration issue causing pod exhaustion

From that experience, we improved:

deployment validation
monitoring alerts
canary deployment strategy
rollback automation

It reinforced the importance of observability, automation, and controlled release strategies in production systems.

Part 2: 
1. What are your daily responsibilities as a DevOps engineer?
2. Which DevOps tools are you proficient with?
3. Can you describe the CI/CD workflow in your project?
4. How do you handle the continuous delivery (CD) aspect in your projects?
5. What methods do you use to check for code vulnerabilities?
6. What AWS services are you proficient in?
7. How would you access data in an S3 bucket from Account A when your application is running on an EC2 instance in Account B?
8. How do you provide access to an S3 bucket, and what permissions need to be set on the bucket side?
9. How can Instance 2, with a static IP, communicate with Instance 1, which is in a private subnet and mapped to a multi-AZ load balancer?
10. For an EC2 instance in a private subnet, how can it verify and download required packages from the internet without using a NAT gateway or bastion host? Are there any other AWS services that can facilitate this?
11. What is the typical latency for a load balancer, and if you encounter high latency, what monitoring steps would you take?
12. If your application is hosted in S3 and users are in different geographic locations, how can you reduce latency?
13. Which services can be integrated with a CDN (Content Delivery Network)?
14. How do you dynamically retrieve VPC details from AWS to create an EC2 instance using IaC?
15. How do you manage unmanaged AWS resources in Terraform?
16. How do you pass arguments to a VPC while using the `terraform import` command?
17. What are the prerequisites before importing a VPC in Terraform?
18. If an S3 bucket was created through Terraform but someone manually added a policy to it, how do you handle this situation using IaC?
19. How do you handle credentials for a PHP application accessing MySQL or any other secrets in Docker?
20. What is the command for running container logs?
21. Have you upgraded any Kubernetes clusters?
22. How do you deploy an application in a Kubernetes cluster?
23. How do you communicate with a Jenkins server and a Kubernetes cluster?
24. How do you generate Kubernetes cluster credentials?
25. Do you only update Docker images in Kubernetes, or do you also update replicas, storage levels, and CPU allocation?
26. What types of pipelines are there in Jenkins?
27. Can you define environment variables inside your Jenkins pipeline?
28. What is the role of artifacts in Jenkins, and why do we need to push them to Nexus instead of building and storing them locally?
29. If you’re developing a Python-based application, how do you separate the packages needed for your local deployment to avoid interfering with globally installed packages?
30. How do you handle error handling in Python?


Part 2:

1. What is GIT stash ?
2. What is a branching strategy?
3. What is the command to discard changes in the working dir?
4. How do you debug the exited container?
5. How do you execute jobs parallely in Jenkins?
6. Maven Lifecycle?
7. How do you upgrade Jenkins?
8. What is called a Parameterised Job in Jenkins?
9. What is called Docker Swarm?
10. How do you handle codes in Nexus satisfactory?
11. How do you manage space issues in the Jenkins server?
12. what is called a multibranch project in the Jenkins server ?
13. How do you secure the Jenkins server?
14. How do you manage GITHUB roles?
15. What is called a NULL resource in Terraform?
16. What is called terraform fmt ?
17.What is called Snowball?
18. How do you manage credentials in Terraform?
19. What is called Code Deploy in AWS?
20. Can you attach a single EBS volume to multiple EC2 instances at the same time?
21. Can you use Multiple FROM in DockerFile ?
22. DockerFile runs as which user?
23. How can we pass an argument to DockerFile?
24. What are deployment strategies?
25. What is called an application load balancer?
26. What is Kubernetes architecture ?
27. What is called Fargate service in AWS?
28. What are Register targets in Ansible?
29. How do you pull artifacts from NEXUS?
30. How to access the S3 bucket privately ?
31. What is the difference between a NAT inst and a NAT Gateway?
32. How can you restrict particular IPs accessing EC2 instances?
33. What is called VPC peering?
34. What is called Transit Gateway?
35. What are the types of autoscaling?
36. To prevent DDOS attacks, which load balancer is used ?
37. What is called a sticky session?
38. What is called Lambda?
39. How do you manage tfstate file in Terraform?
40. How do yo create multiple ec2 instances in terraform ?
41. AWS has released a new service, how does Terraform behave?
42. How do you uncommit the changes that have already been pushed to GitHub?
43. What is the difference between git pull and git fetch?
44. What is called Jenkins File?
45. What is called Shared Libraries in Jenkins?
46. What is called docker networking?
47. What is called a Trust relationship in AWS?
48. What is called Public Subnet and Private Subnet?
49. How do you establish a connection between ec2 instance to another ec2 instance?
50. What is realm command ?
51. How do you differentiate within an AWS account dev env, test env, and prod env?
52. Types of ec2 instances?
53. How can you encrypt the already created unencrypted EBS without creating a fresh EC2 instance?
54. How do you install Nginx in the Ansible playbook?
55. How do you recover the deleted object in S3?

# part3: CI/CD and DevOps Practice

1. What's the minimum requirement to set up CI/CD pipelines in Azure DevOps using a GitHub source code repository.
2. How do you manage environment-specific configurations in a CI/CD pipeline.
3. How does Jenkins foster collaboration between development and operations teams, and how do you handle conflicts?
4. Explain the blue-green deployment, canary deployment, and rollback processes with real-time scenarios.
5. What are the advantages and disadvantages of using feature flags in CI/CD
6. How do you ensure compliance in a DevSecOps pipeline. 

# Part 4: Cloud and Infrastructure:

1. What is meant by geolocation-based routing and latency-based routing, and which AWS service helps in configuring such routing policies? Explain a scenario.

2. What are some best practices for organizing Terraform configurations to ensure they are modular and reusable

3. Explain the Git branching strategy and how it supports collaboration in software development.

4. How would you implement security controls in a CI/CD pipeline.

5. vulnerabilities found during security scans in a continuous delivery pipeline

6. What security considerations do you take into account when using Infrastructure as Code (IaC)How do you secure your IaC templates
